# tests/e2e/

Reserved for future integration/smoke tests (e.g. full request/response
or browser-driven flows). Existing unit tests stay colocated with the
code they cover (`*.test.ts`/`*.test.tsx`, auto-discovered by Vitest) —
don't move them here.
